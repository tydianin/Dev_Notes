"use strict";
// Giving the right name.

// Create a variable with the name of our planet. How would you name such a variable?
let localPlanetThree = "Earth";
// depending on the context, the variable name may change but should still be descriptive.

// Create a variable to store the name of the current visitor to a website. How would you name such a variable?
let currentUser = get();
// Depending on the context, the variable and assignment may change but should still be clear.